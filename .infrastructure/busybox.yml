apiVersion: v1
kind: Pod
metadata:
  name: busybox
  namespace: todoapp
spec:
  containers:
    - name: busybox
      image: ikulyk404/busyboxplus:curl
      command: ["/bin/sh", "-c", "while true; do sleep 3600; done"]
